#!/usr/bin/env groovy

pipeline {
    agent any
    stages {
        stage('Example') {
            steps {
                echo 'Testing'
                script {
                    def browser = 'chrome'
                }
            }
        }
    }
}